apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/devops-dil/alienapp:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/devops-dil/alienapp:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: alienapp
        chartPath: charts/alienapp
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/devops-dil/alienapp'
          image.tag: '{{.DIGEST_HEX}}'
